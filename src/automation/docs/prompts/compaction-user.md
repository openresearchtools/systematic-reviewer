Build the next Active Memory from the material below.

This is a state-reduction pass for a long-running Systematic Reviewer project, not a casual summary. Read the prior Active Memory first, then read the new chronological material as the freshest project history. Produce one replacement Active Memory that preserves the project state needed for future autonomous agent calls.

Use this merge procedure:

1. Identify the newest active objective and its origin: user, API, Auto Drive, reviewer, steer message, or workflow follow-up.
2. Reconstruct the recent causal sequence: what happened, in what order, against which scope, with which outputs or failures.
3. Update workflow state: harvest, Pending/Excluded/Excluded FT/Included collections, embeddings, semantic search, screening, retrieval, markdown conversion, extraction templates/runs, Explore, PRISMA, report writing, jobs, and project files.
4. Preserve unresolved task stacks, blockers, waiting jobs, failed calls, retry-sensitive state, and the next recovery step.
5. Preserve exact handles: paths, item keys, collection keys, attachment keys, job ids, run ids, search ids, session ids, template names, score columns, report sections, DOI/citation/version/license/URL rules.
6. Move completed work into Completed, keep still-relevant older facts in Older History, and keep only actionable unfinished work in Open Tasks.
7. Ensure the final Next Best Action follows from the latest objective plus the newest project state.

Do not collapse complex work into vague statements. If the input contains detailed systematic-review workflow actions, keep the meaningful details. If it contains website/repository/plugin-release work, preserve the exact citation, DOI, license, URL, versioning, workflow, or file rules that affect future edits.

Return only the replacement Active Memory markdown, using the required section headings.
